[[ $- != *i* ]] && return

source ~/.local/share/omarchy/default/bash/rc

append_path() {
  case ":$PATH:" in
    *:"$1":*) ;;
    *) PATH="${PATH:+$PATH:}$1"
  esac
}

append_path "/opt/android-sdk/cmdline-tools/latest/bin"

export PATH
